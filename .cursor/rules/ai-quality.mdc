---
description: Mandatory evidence-based delivery workflow
alwaysApply: true
---

Read `.ai-quality/agent-policy.md`, then run `pwsh ./aq.ps1 status` before editing. Never implement before `implementation-authorized`. Never self-approve in manual mode; in trusted mode self-approve only through the workflow CLI after readiness checks pass. Never claim completion without passing the Full quality gate and delivery validator.
