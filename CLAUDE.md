# Mandatory workflow

Read and obey `.ai-quality/agent-policy.md` before editing. Run `pwsh ./aq.ps1 status` at the start and after compaction/resume. Use the active work item in `.ai-quality/active-work-item.txt`. Do not implement before `implementation-authorized`. In manual mode do not self-approve; in trusted mode use the workflow CLI to self-approve only complete artifacts. Do not claim completion without a passing Full gate and delivery validation.
